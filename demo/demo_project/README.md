# Happy RNA-seq Demo Project

This is a tiny project skeleton for the Happy RNA-seq template-based local-to-HPC runner.

Add FASTQ files to `input/fastq/`, edit `configs/configure.yaml`, generate or review `input/samplesheet.csv`, then validate before upload.
