# Happy RNA-seq Template Project

Add your FASTQ files to `input/fastq/`, reference genome to `input/reference/`, annotation to `input/annotation/`, then run the scripts in order.

Start locally with:

```bash
bash scripts/make_samplesheet.sh
bash scripts/input_validate.sh
bash scripts/configure.sh
bash scripts/upload_to_hpc.sh
```
